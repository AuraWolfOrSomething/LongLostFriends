//Requires AoE by Vesly

#include "GuardianInternal.event"

#include "asm/prGuardianDefRes.lyn.event"
ALIGN 4

GuardianAoeMap:
BYTE 5 5 // Size (1-indexed, so this is a 5x5y array) 
BYTE 2 2 // Center (0-indexed, so [2,2] is the center)
BYTE 0 0 1 0 0
BYTE 0 1 1 1 0
BYTE 1 1 1 1 1
BYTE 0 1 1 1 0
BYTE 0 0 1 0 0
ALIGN 4
